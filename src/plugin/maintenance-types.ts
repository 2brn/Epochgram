export type RebuildSelection = {
	index: boolean;
	semantics: boolean;
	topics: boolean;
	aiSummaries: boolean;
	epochs: boolean;
};

export type ResetSelection = {
	settings: boolean;
	search: boolean;
	dataFiles: boolean;
	marks: boolean;
	reviewState: boolean;
	pinned: boolean;
	semantics: boolean;
	topics: boolean;
	trackedChanges: boolean;
	aiSummaries: boolean;
	epochs: boolean;
};
